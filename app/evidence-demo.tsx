"use client";
import { useState } from "react";

const steps=["Evidence review","Evidence graph","SBOM impact","Incident draft","Dossier"];
type RunState="idle"|"loading"|"success"|"error";
type Suggestion={targetField:string;proposedValue:string;sourceFragmentId:string;confidence:number;warnings:string[]};
type ModelMeta={model:string;responseId:string;status:string};
type Impact={explanation:string;reviewPriorities:string[];citedNodeIds:string[]};
type Incident={fields:Record<string,string|null>;missingInformation:string[];citations:string[]};
const authHeaders={"x-cra-demo-user":"aegis-labs-judge"};

export function EvidenceDemo(){
 const[step,setStep]=useState(0);const[approved,setApproved]=useState(false);const[artifactId,setArtifactId]=useState<string>();const[upload,setUpload]=useState("Upload or load the approved synthetic document");
 const[suggestion,setSuggestion]=useState<Suggestion>();const[suggestionId,setSuggestionId]=useState<string>();const[extractState,setExtractState]=useState<RunState>("idle");const[extractMessage,setExtractMessage]=useState("No model output yet.");const[meta,setMeta]=useState<ModelMeta>();
 const[impact,setImpact]=useState<Impact>();const[impactState,setImpactState]=useState<RunState>("idle");const[impactMessage,setImpactMessage]=useState("Run GPT-5.6 after the deterministic graph calculation.");
 const[incident,setIncident]=useState<Incident>();const[incidentState,setIncidentState]=useState<RunState>("idle");const[incidentMessage,setIncidentMessage]=useState("Generate a worksheet from approved facts only.");
 async function uploadArtifact(file?:File){if(!file)return;setUpload("Validating and storing…");setSuggestion(undefined);setApproved(false);try{const body=new FormData();body.set("file",file);const response=await fetch("/api/v1/artifacts",{method:"POST",headers:authHeaders,body});const data=await response.json();if(!response.ok)throw new Error(data.error);setArtifactId(data.artifact.id);setUpload(`Stored · SHA-256 ${data.artifact.sha256.slice(0,12)}…`)}catch(error){setArtifactId(undefined);setUpload(error instanceof Error?error.message:"Upload failed")}}
 async function loadSynthetic(){const response=await fetch("/demo-export/technical-dossier.pdf");const blob=await response.blob();await uploadArtifact(new File([blob],"architecture-v2.3.pdf",{type:"application/pdf"}))}
 async function extract(){if(!artifactId)return;setExtractState("loading");setExtractMessage("GPT-5.6 is grounding a structured suggestion…");setSuggestion(undefined);setApproved(false);try{const response=await fetch("/api/v1/ai/evidence",{method:"POST",headers:{...authHeaders,"Content-Type":"application/json"},body:JSON.stringify({artifactId,sourceFragmentId:"architecture-v2.3:p4:p2"})});const data=await response.json();if(!response.ok)throw new Error(data.error);setSuggestion(data.output);setSuggestionId(data.suggestionId);setMeta({model:data.model,responseId:data.responseId,status:data.status});setExtractState("success");setExtractMessage("Schema-valid live response received. Human review is still required.")}catch(error){setExtractState("error");setExtractMessage(error instanceof Error?error.message:"Live extraction failed")}}
 async function approve(){if(!suggestionId)return;try{const response=await fetch("/api/v1/ai/approve",{method:"POST",headers:{...authHeaders,"Content-Type":"application/json"},body:JSON.stringify({suggestionId})});const data=await response.json();if(!response.ok)throw new Error(data.error);setApproved(data.status==="APPROVED")}catch(error){setExtractState("error");setExtractMessage(error instanceof Error?error.message:"Approval failed")}}
 async function explainImpact(){setImpactState("loading");setImpactMessage("GPT-5.6 is explaining the code-computed blast radius…");try{const response=await fetch("/api/v1/ai/impact",{method:"POST",headers:{...authHeaders,"Content-Type":"application/json"},body:JSON.stringify({impactCaseId:"sbom-2.3-2.4"})});const data=await response.json();if(!response.ok)throw new Error(data.error);setImpact(data.output);setImpactState("success");setImpactMessage(`${data.model} · ${data.responseId}`)}catch(error){setImpactState("error");setImpactMessage(error instanceof Error?error.message:"Impact explanation failed")}}
 async function draftIncident(){setIncidentState("loading");setIncidentMessage("GPT-5.6 is drafting from approved facts…");try{const response=await fetch("/api/v1/ai/incident",{method:"POST",headers:{...authHeaders,"Content-Type":"application/json"},body:JSON.stringify({incidentId:"incident-aegis-001"})});const data=await response.json();if(!response.ok)throw new Error(data.error);setIncident(data.output);setIncidentState("success");setIncidentMessage(`${data.model} · ${data.responseId} · SUGGESTED, not submitted`)}catch(error){setIncidentState("error");setIncidentMessage(error instanceof Error?error.message:"Incident draft failed")}}
 return <main><aside><div className="brand"><span>CE</span><div>CRA Evidence OS<small>Build Week demo</small></div></div><nav aria-label="Demo workflow">{steps.map((s,i)=><button key={s} className={step===i?"active":""} onClick={()=>setStep(i)}><b>0{i+1}</b>{s}</button>)}</nav><div className="guardrail">AI assists. People decide.<small>This measures evidence readiness—not legal compliance.</small></div></aside>
 <section className="shell"><header><div><small>AEGIS LABS / CONNECTED PRODUCTS</small><h1>AegisEdge Gateway <em>v2.3</em></h1></div><div className="metrics" aria-label="Operational evidence status"><span><b>12</b>approved claims</span><span className="warn"><b>2</b>stale items</span><span><b>1</b>incident field missing</span><span><b>4</b>export files verified</span></div></header>
 {step===0&&<article><section className="hero"><div><div className="eyebrow">CRA EVIDENCE CONTROL PLANE</div><h2>When your product changes, can you prove what is still true?</h2><p>For EU software, IoT and industrial-equipment manufacturers, scattered documents turn every release and security incident into a manual evidence hunt. CRA Evidence OS connects claims to exact sources, detects stale evidence and prepares review-ready records.</p><div className="hero-tags"><span>24h early warning</span><span>72h notification facts</span><span>Human approval required</span></div></div><div className="before-after"><section><label>BEFORE</label><b>4–8 hours</b><p>Search documents, reconcile versions and rebuild citations.</p></section><i>→</i><section><label>WITH EVIDENCE OS</label><b>Minutes</b><p>Review grounded suggestions and only the claims affected by change.</p></section></div></section><div className="eyebrow">EVIDENCE INBOX · 1 NEEDS REVIEW</div><h2>Document → GPT-5.6 → cited suggestion → human decision</h2><p>The model proposes; the reviewer compares it with the exact source and owns the decision.</p><div className="intake-row"><label className="uploader">UPLOAD EVIDENCE<input aria-label="Upload evidence PDF or JSON" type="file" accept="application/pdf,application/json,.json" onChange={e=>uploadArtifact(e.target.files?.[0])}/><span>{upload}</span></label><button className="reject" onClick={loadSynthetic}>Load synthetic document</button><button className="live-run" onClick={extract} disabled={!artifactId||extractState==="loading"}>{extractState==="loading"?"Extracting…":"Extract with GPT-5.6"}</button></div><p className={`ai-status ${extractState}`}>{extractState.toUpperCase()} · {extractMessage}</p><div className="split"><section className="source"><label>KNOWN SOURCE FRAGMENT · PAGE 4</label><h3>AegisEdge security architecture</h3><p>“All administrative traffic is protected using <mark>mutual TLS 1.3. Device identities are provisioned during manufacturing and rotated every 90 days.</mark> Failed authentication attempts are rate limited and recorded.”</p><footer>architecture-v2.3.pdf · architecture-v2.3:p4:p2</footer></section><section className="suggestion"><label>{suggestion?"LIVE GPT-5.6 OUTPUT":"AWAITING LIVE EXTRACTION"}</label>{suggestion?<><h3>{suggestion.targetField}</h3><p>{suggestion.proposedValue}</p><dl><div><dt>Source</dt><dd>{suggestion.sourceFragmentId}</dd></div><div><dt>Confidence</dt><dd>{suggestion.confidence.toFixed(2)}</dd></div><div><dt>Model</dt><dd>{meta?.model}</dd></div><div><dt>Response</dt><dd className="response-id">{meta?.responseId}</dd></div><div><dt>Status</dt><dd className={approved?"ok":"warn"}>{approved?"APPROVED":"SUGGESTED"}</dd></div></dl><div className="actions"><button className="reject" onClick={()=>setApproved(false)}>Reject</button><button className="approve" onClick={approve} disabled={approved}>Approve claim</button></div></>:<div className="empty-state"><b>No GPT result is being simulated.</b><p>Upload the synthetic document and run the server-side extraction.</p></div>}</section></div></article>}
 {step===1&&<article><div className="eyebrow">EVIDENCE GRAPH</div><h2>Every claim has a verifiable lineage</h2><div className="graph"><div>CRA REQUIREMENT<small>Annex I Part I (3)(d)</small></div><i>→</i><div className={approved?"good":"pending"}>CLAIM<small>{approved?"Approved by authenticated reviewer":"No approved live suggestion"}</small></div><i>→</i><div>SOURCE FRAGMENT<small>Page 4 · paragraph 2</small></div><i>→</i><div>ARTIFACT<small>architecture-v2.3.pdf</small></div></div><p className="note">Approval is append-only and records reviewer, source version, timestamp and model run.</p></article>}
 {step===2&&<article><div className="eyebrow">SBOM DELTA · VERSION 2.3 → 2.4</div><h2>One component changed. Two claims need attention.</h2><p>Application code has already computed the blast radius. GPT-5.6 can explain review priority, but cannot alter the stale set.</p><div className="delta"><b>openssl</b><del>3.0.12</del><span>→</span><ins>3.0.14</ins><strong>UPDATED</strong></div><div className="cards"><div><span className="stale">STALE</span><h3>Secure administrative communications</h3><p>Linked through the TLS runtime dependency.</p></div><div><span className="stale">STALE</span><h3>Cryptographic update policy</h3><p>Evidence references the previous component snapshot.</p></div><div><span className="ok">CURRENT</span><h3>Access logging</h3><p>No graph path to the changed component.</p></div></div><button className="live-run inline-run" onClick={explainImpact} disabled={impactState==="loading"}>{impactState==="loading"?"Explaining…":"Explain deterministic impact with GPT-5.6"}</button><p className={`ai-status ${impactState}`}>{impactMessage}</p>{impact&&<section className="model-result"><label>GPT-5.6 EXPLANATION · SUGGESTED</label><p>{impact.explanation}</p><b>Review priorities</b><ul>{impact.reviewPriorities.map(item=><li key={item}>{item}</li>)}</ul><small>Cited graph nodes: {impact.citedNodeIds.join(", ")}</small></section>}</article>}
 {step===3&&<article><div className="eyebrow">INCIDENT WORKSHEET · HUMAN REVIEW REQUIRED</div><h2>24h / 72h reporting facts, prepared—not submitted</h2><div className="deadline-strip"><b>24h</b><span>Early warning facts</span><i>→</i><b>72h</b><span>Notification details</span></div><div className="worksheet"><div><label>Product</label><b>AegisEdge Gateway 2.3</b></div><div><label>Awareness time</label><b>2026-07-19 08:15 UTC</b></div><div><label>Incident summary</label><b>Authentication bypass under investigation</b></div><div><label>Exploitation observed</label><b className="warn">Unknown · confirmation required</b></div></div><button className="live-run inline-run" onClick={draftIncident} disabled={incidentState==="loading"}>{incidentState==="loading"?"Drafting…":"Draft worksheet with GPT-5.6"}</button><p className={`ai-status ${incidentState}`}>{incidentMessage}</p>{incident&&<section className="model-result"><label>GPT-5.6 INCIDENT DRAFT · SUGGESTED</label><dl>{Object.entries(incident.fields).map(([key,value])=><div key={key}><dt>{key}</dt><dd>{value??"Unknown"}</dd></div>)}</dl><b>Missing information</b><ul>{incident.missingInformation.map(item=><li key={item}>{item}</li>)}</ul><small>Approved fact citations: {incident.citations.join(", ")}</small></section>}<p className="note">No ENISA API call is made. Export is copy-ready and requires explicit human submission.</p></article>}
 {step===4&&<article><div className="eyebrow">ANNEX VII MINI-DOSSIER</div><h2>A reproducible package with integrity metadata</h2><div className="manifest"><div><b>technical-dossier.pdf</b><code>SHA-256 in manifest</code></div><div><b>evidence-index.json</b><code>Approved source lineage</code></div><div><b>sbom.cdx.json</b><code>CycloneDX 1.6</code></div><div><b>manifest.json</b><code>4 files · DRAFT</code></div></div><div className="actions"><a className="reject download" href="/demo-export/technical-dossier.pdf" download>Download PDF</a><a className="approve download" href="/demo-export/aegisedge-annex-vii-draft.zip" download>Download ZIP package</a></div><p className="note">DRAFT — NOT APPROVED. Stale or unsupported claims remain visibly flagged.</p></article>}
 <footer className="stepper"><span>Demo workflow</span><b>{step+1} / {steps.length}</b><button disabled={step===steps.length-1} onClick={()=>setStep(Math.min(steps.length-1,step+1))}>Next step →</button></footer></section></main>
}
